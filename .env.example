# Redrob VectorDB does not read API keys from environment files.
# Connect in the app so the key is stored in the OS credential manager.

# Optional Rust logging level for local development:
RUST_LOG=redrob_vectordb=info
